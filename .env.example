# ===== Server Core =====
# Port your Express app runs on (5000 by default for local dev)
PORT=5000

# Environment mode: "development" or "production"
NODE_ENV=development


# ===== Database =====
# Your MongoDB Atlas connection URI.
# Example:
# ***REMOVED***cluster0.xxxxx.mongodb.net/authdb
MONGO_URI=***REMOVED***cluster.mongodb.net/authdb


# ===== JWT =====
# Long, random string used to sign access + refresh tokens.
# Generate one with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<your_jwt_secret>


# ===== CORS (Allowed Origins) =====
# Your frontend URLs for local dev and production.
# - ORIGIN_DEV is your Vite dev server (default)
# - ORIGIN_PROD is your deployed frontend (Vercel, etc.)
ORIGIN_DEV=http://localhost:5173
ORIGIN_PROD=https://yourdomain.com

EMAIL_FROM=no-reply@yourdomain.com
RESEND_API_KEY=<your_resend_api_key>

# client/.env.example
VITE_API_BASE_URL=http://localhost:5000/api
